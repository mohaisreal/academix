---
import type { HTMLAttributes } from "astro/types";
import { tv } from "tailwind-variants";

import { ButtonVariants } from "@/components/starwind/button";

type Props = HTMLAttributes<"button"> & {
    /**
   * Cuando es true, el componente renderizará su elemento hijo en vez de un botón
   */
  asChild?: boolean;
};

const { class: className, asChild = false, ...rest } = Astro.props;

// Obtiene el primer elemento hijo si asChild es true
let hasChildren = false;
if (Astro.slots.has("default")) {
  hasChildren = true;
}
---

{
  asChild && hasChildren ? (
    <div
      class:list={["starwind-alert-dialog-close", className]}
      data-slot="alert-dialog-cancel"
      data-as-child
    >
      <slot />
    </div>
  ) : (
    <button
      type="button"
      class={ButtonVariants.button({
        variant: "outline",
        class: `starwind-alert-dialog-close ${className || ""}`,
      })}
      data-slot="alert-dialog-cancel"
      {...rest}
    >
      <slot />
    </button>
  )
}
